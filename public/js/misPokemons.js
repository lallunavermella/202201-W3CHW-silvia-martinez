import PageComponentMisPokemons from "./components/PageComponentMisPokemons.js";

const parentElement = document.querySelector("body");
const page = new PageComponentMisPokemons(parentElement, "page");

export default page;
